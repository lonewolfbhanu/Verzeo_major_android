name: navigation_drawer_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_auth:
  firebase_core:
  cloud_firestore:
  google_sign_in:
  razorpay_flutter: ^1.2.7
  provider:
dev_dependencies:

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true